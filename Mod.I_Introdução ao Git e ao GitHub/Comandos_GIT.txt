git init
git add
git commit
git status

ls
ls -a

git config --global user.e-mail "mjsantos.santos@hotmail.com"

git config --global user.name moisesjsantos

git add nomeArquivo
git add *
git add .

git commit -m "Commit vários arquivos do repositorio projeto inicial"

git push
git push origin master	: envia para repositorio on-line

git pull origin master	: puchar o repositorio on-line

git remote -v : verifica para onde o repositorio esta apontado
